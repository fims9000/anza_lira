# Fast smoke test (1 epoch, small batch)
dataset: cifar10
data_root: ./data
val_fraction: 0.1
variant: baseline
num_rules: 4

epochs: 1
batch_size: 32
lr: 0.001
weight_decay: 0.0001
seed: 0
num_workers: 0

results_dir: ./results
run_name: null
device: null

timing_warmup: 1
timing_iters: 2

